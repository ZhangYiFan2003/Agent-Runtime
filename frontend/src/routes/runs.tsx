import { ListTree } from "lucide-react";
import { PlaceholderPage } from "../components/placeholder";

export function RunsPage() {
  return <PlaceholderPage icon={ListTree} title="Runs" phase="phase 2 · dense run table" />;
}
