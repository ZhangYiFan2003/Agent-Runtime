import { Boxes } from "lucide-react";
import { PlaceholderPage } from "../components/placeholder";

export function WorkbenchPage() {
  return <PlaceholderPage icon={Boxes} title="Workbench" phase="phase 2 · thread + turn + live feed" />;
}
